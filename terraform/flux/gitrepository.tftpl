apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: ${name}
  namespace: ${namespace}
spec:
  interval: 1m0s
  ref:
    branch: ${branch}
  secretRef:
    name: ${secret}
  url: ${url}